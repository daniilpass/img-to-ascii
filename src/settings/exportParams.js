export const exportParmas = {
    htmlTemplate: `<html>
        <body>
            <div style="font-family: ${font};
                font-size: ${fontSize}px;
                line-height: ${lineHeight}px;
                color: ${color};
                background-color: ${backgroundColor};
                white-space: nowrap;"
            >
            ${asciiImage.html}
            </div>
        </body>
    </html>`
}